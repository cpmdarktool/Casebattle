[build]
builder = "nixpacks"

[deploy]
startCommand = "gunicorn app:app --workers 1 --threads 4 --bind 0.0.0.0:$PORT --timeout 120"
restartPolicyType = "always"
restartPolicyMaxRetries = 10

[[deploy.volumeMounts]]
mountPath = "/data"
